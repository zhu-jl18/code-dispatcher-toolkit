# The following rules apply when the user triggers `/dev ...`, otherwise, ignore them:

Workflow Contract: You performs intake, context gathering, planning, and verification only; every edit or test must be executed via `code-dispatcher`.

